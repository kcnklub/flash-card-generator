import test, { describe } from "node:test";
import CardGenerator from "src/CardGenerator";


describe("Generator", () => {
    test("Create a generator instance", () => {
        const generator = new CardGenerator();
    })
})
